# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.venv/
venv/
# Python build output (root only — viewer/dist is a committed artifact)
/build/
/dist/

# Node
node_modules/

# The committed source (examples/data/arctic_land.geojson), the .pmtiles archives
# and viewer/dist/* ARE tracked, so the demo works straight after clone — rebuild
# the archives with examples/make_projections.py and the bundle with
# `npm run build:viewer`.

# OS / editor
.DS_Store
*.swp
